# =============================================================================
# jdoo - Universal Odoo Docker Environment
# =============================================================================
# Works with both ./jdoo wrapper (local dev) and plain docker compose (Dokploy).
#
# Local development:
#   cp .env.example .env && nano .env
#   ./jdoo up -d --build          # auto-computes PYTHON_VERSION, PG_VERSION, etc.
#
# Dokploy / direct docker compose:
#   Set ODOO_VERSION, then run: bash compute-env.sh && docker compose up -d --build
#   compute-env.sh auto-computes PYTHON_VERSION + PG_VERSION from ODOO_VERSION
# =============================================================================

# =============================================================================
# VERSION MATRIX
# =============================================================================
# Set ODOO_VERSION (required). The rest is auto-computed by ./jdoo or compute-env.sh.
#
#  ODOO_VERSION | PYTHON_VERSION | PG_VERSION | COMPOSE_PROJECT_NAME*
# --------------|----------------|------------|----------------------
#  15.0         | 3.10           | 14         | jdoo15
#  16.0         | 3.10           | 15         | jdoo16
#  17.0         | 3.12           | 16         | jdoo17
#  18.0         | 3.12           | 16         | jdoo18
#  19.0         | 3.12 (default) | 17 (default)| jdoo19
#
# * COMPOSE_PROJECT_NAME is only auto-computed by ./jdoo.
#   ODOO_PORT defaults to 8069 (override in .env).
# =============================================================================

# -----------------------------------------------------------------------------
# Odoo Version (required)
# -----------------------------------------------------------------------------
ODOO_VERSION=19.0

# -----------------------------------------------------------------------------
# Version-dependent settings (auto-computed by ./jdoo or compute-env.sh)
# Uncomment to override the auto-computed values
# -----------------------------------------------------------------------------
#PYTHON_VERSION=3.12
#PG_VERSION=17
#ODOO_PORT=8069
#COMPOSE_PROJECT_NAME=jdoo

# -----------------------------------------------------------------------------
# Odoo Source Repository
# -----------------------------------------------------------------------------
# Default: autocme/oc (optimized minimal core, ~80% smaller)
# Alternative: https://github.com/odoo/odoo.git (official, much larger)
ODOO_REPO=https://github.com/autocme/oc.git

# -----------------------------------------------------------------------------
# Security - CHANGE THESE IN PRODUCTION!
# -----------------------------------------------------------------------------
POSTGRES_USER=odoo
POSTGRES_PASSWORD=odoo
ODOO_ADMIN_PASSWORD=ikarout1998

# -----------------------------------------------------------------------------
# Container Configuration
# -----------------------------------------------------------------------------
LONGPOLLING_PORT=8072

# User/Group IDs (match host user for volume permissions)
PUID=1000
PGID=1000

# -----------------------------------------------------------------------------
# Resource Allocation (auto-computed inside containers)
# -----------------------------------------------------------------------------
# Odoo workers (auto: CPU * 2, min 2 — capped by RAM/325MB per worker)
# On big hosts (e.g. 32 cores -> 64 workers) auto is oversized for a
# single-instance stack: slower first responses (each worker cold-loads the
# registry on its first request) and ~150MB idle RSS per worker.
# For SaaS instance-per-stack deploys, set an explicit value (rule of thumb:
# workers ~= expected concurrent users / 6, e.g. 8):
#WORKERS=8

# Cron threads (auto: 1 if workers < 6, else 2)
#MAX_CRON_THREADS=

# Per-worker memory limits in bytes (auto: based on available RAM)
#LIMIT_MEMORY_SOFT=
#LIMIT_MEMORY_HARD=

# Resource watcher - monitors CPU/RAM changes and restarts Odoo (TRUE/FALSE)
#RESOURCE_WATCHER=FALSE

# -----------------------------------------------------------------------------
# PostgreSQL Tuning (auto-computed inside PG container from RAM)
# Override any value here to skip auto-computation for that setting.
# -----------------------------------------------------------------------------
#PG_SHARED_BUFFERS=
#PG_EFFECTIVE_CACHE_SIZE=
#PG_WORK_MEM=64MB
#PG_MAINTENANCE_WORK_MEM=
#PG_MAX_CONNECTIONS=100

# -----------------------------------------------------------------------------
# Odoo Features
# -----------------------------------------------------------------------------
# Auto-upgrade modules on container restart (TRUE/FALSE)
AUTO_UPGRADE=FALSE

# Skip Odoo core addons during upgrade (TRUE/FALSE)
# TRUE = faster, only upgrades custom/third-party addons
UPGRADE_IGNORE_CORE=TRUE

# Fix report.url for wkhtmltopdf PDF rendering inside the container (TRUE/FALSE)
# When TRUE, sets report.url = http://localhost:<port> in all databases
# so wkhtmltopdf can fetch CSS/JS assets locally for PDF generation
FIX_REPORT_URL=FALSE

# -----------------------------------------------------------------------------
# Database Initialization (INIT_* variables)
# -----------------------------------------------------------------------------
# All 8 Odoo DB creation fields supported via XML-RPC.
# Leave INIT_DB empty to skip auto-creation and use the Odoo UI instead.

# Database name (required to enable auto-creation)
INIT_DB=

# Admin login email
INIT_LOGIN=admin

# Admin password
INIT_PASSWORD=ikarout1998

# Language code (e.g., en_US, ar_001, fr_FR)
INIT_LANG=en_US

# Country code (e.g., US, SA, FR) — empty = auto-detect
INIT_COUNTRY=

# Admin phone number (e.g., +966500000000) — empty = skip
INIT_PHONE=

# Load demo data (TRUE/FALSE)
INIT_DEMO=FALSE

# Extra modules to install after DB creation (comma-separated)
INIT_MODULES=

# Legacy fallback (used only if INIT_DB is empty)
# Example: "-n odoo_prod -m base,web --unless-initialized"
INITDB_OPTIONS=

# Target database name (empty = auto-detect first non-system DB)
ODOO_DB_NAME=

# -----------------------------------------------------------------------------
# Runtime Package Installation
# -----------------------------------------------------------------------------
# Python packages (comma-separated, checked on each startup)
# Includes external_dependencies of all CUA custom modules
# asyncssh pinned to 2.21.1: newer versions force cryptography>=48 which breaks
# Odoo's pinned pyopenssl (import OpenSSL fails -> base module cannot load)
# (requests/cryptography/psycopg2/qrcode/reportlab/polib already ship with Odoo requirements.txt)
# NOTE: All packages below are pre-baked into the jdoo image (Odoo 17+; on 15/16
# asyncssh/pdfminer.six/pdfplumber are runtime-installed due to the cryptography
# ==3.4.8 pin). This list is a fast presence check + fallback — keep it in sync
# with CUA module external_dependencies. Add future extras here; no rebuild needed.
PY_INSTALL=phonenumbers,python-stdnum,num2words,PyJWT,asyncssh==2.21.1,disposable-email-domains,email-validator,feedparser,hijridate,html2text,httpx,pdfminer.six,pdfplumber,rapidfuzz,temporalio

# NPM packages (comma-separated, checked on each startup)
NPM_INSTALL=rtlcss,less

# -----------------------------------------------------------------------------
# JCICD Integration
# -----------------------------------------------------------------------------
# The `repos` external volume is mounted at /repos (read-only) in docker-compose.yml.
# JCICD syncs repositories (e.g. autocme/oa) into /repos/{version}/{repo}.
# See: https://github.com/autocme/JCICD
